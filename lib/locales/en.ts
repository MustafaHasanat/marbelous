import { globalEN } from "./data/global";
import { guestEN } from "./data/guest";
import { toastEN } from "./data/toast";

const en = {
    global: globalEN,
    toast: toastEN,
    guest: guestEN,
};

export default en;
